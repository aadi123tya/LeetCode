class Solution {
    static int gameOfXor(int N , int[] A) {
       if(N%2==0)return 0;
       int res =0;
       for(int i=0;i<N;i++)
       {
           if(i%2==0)
           {
               res^=A[i];
           }
       }
       return res;
    }
};
